# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only dev-guide files — never pushed to the public repo
CLAUDE.md
RESUME_PROMPT.md

# seed-gen: fetched/scraped intermediates, never committed — only the
# resulting resources/static.db is. Re-derivable by re-running the pipeline.
tools/seed-gen/.cache
tools/seed-gen/out
src-tauri/resources/.static-db-build-*
